import { VerifphonePipe } from './verifphone.pipe';

describe('VerifphonePipe', () => {
  it('create an instance', () => {
    const pipe = new VerifphonePipe();
    expect(pipe).toBeTruthy();
  });
});
